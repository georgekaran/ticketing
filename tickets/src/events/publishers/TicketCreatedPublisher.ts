import {Publisher, Subjects, TicketCreatedEvent} from '@gmtickets/common';

export class TicketCreatedPublisher extends Publisher<TicketCreatedEvent> {
    subject: Subjects.TicketCreated = Subjects.TicketCreated;
}